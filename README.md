# SampleSite — GitHub Pages Demo

This is a simple static site (HTML/CSS/JS) designed as a starter template for GitHub Pages.

Features:
- Responsive layout
- Dark / light theme (persisted)
- Small todo app saved to `localStorage`
- Fetch demo (random joke)
- Easy to deploy on GitHub Pages

## How to run locally
Open `index.html` in a browser. For local development with a simple server:
```bash
# python 3.x
python -m http.server 8000
# then open http://localhost:8000
